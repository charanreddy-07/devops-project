apiVersion: v1
kind: Service
metadata:
  name: devops-service
  namespace: devops
spec:
  type: ClusterIP
  selector:
    app: devops-project
  ports:
    - name: http
      port: 80
      targetPort: 3000
